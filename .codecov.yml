coverage:
  status:
    project:
      default:
        # Allow a slightly larger drop for mechanical/refactor PRs whose changed
        # lines are exercised by the acceptance suite (not the unit-test upload).
        threshold: 2%
    patch:
      default:
        target: 50%

ignore:
  - "vendor/**"
  - "main.go"
